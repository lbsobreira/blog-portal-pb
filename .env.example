# ===========================================
# Blog Portal - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# Never commit your .env file to version control!

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# Local development example:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/blog_portal"
#
# Vercel Postgres / Neon / Supabase example:
# DATABASE_URL="postgresql://user:pass@host.provider.com:5432/dbname?sslmode=require"
#
DATABASE_URL="postgresql://user:password@localhost:5432/blog_portal"

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# Auth.js (NextAuth v5) configuration

# The URL where your app is hosted
# Local: http://localhost:3000
# Production: https://yourdomain.com
AUTH_URL="http://localhost:3000"

# Secret key for encrypting tokens and sessions
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
AUTH_SECRET="your-secret-key-here-generate-with-openssl"

# ===========================================
# EMAIL SERVICE (Required for magic links)
# ===========================================
# Using Resend (https://resend.com) for transactional emails
# Free tier: 3,000 emails/month

# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxx"

# The email address magic links will be sent from
# Must be verified in Resend or use their test domain
# Development: onboarding@resend.dev (works without verification)
# Production: noreply@yourdomain.com (requires DNS verification)
EMAIL_FROM="onboarding@resend.dev"

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Force using Resend even in development
# Set to "true" to always send real emails
# FORCE_RESEND="true"
# NEXT_PUBLIC_FORCE_RESEND="true"

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
#
# Vercel:
#   - Add all variables in Project Settings > Environment Variables
#   - AUTH_URL should be your production URL
#   - Vercel Postgres can be added via Integrations
#
# Railway/Render:
#   - Similar setup through their environment variable UI
#   - They provide managed PostgreSQL databases
#
# Self-hosted:
#   - Ensure PostgreSQL is accessible
#   - Set up reverse proxy (nginx) for HTTPS
#   - AUTH_URL must match your domain exactly
